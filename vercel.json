{
    "version": 2,
    "builds": [
        {
            "src": "./nfce-api.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "nfce-api.py"
        }
    ],
    "regions": ["gru1"]
}